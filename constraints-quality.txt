# constraints-quality.txt — SINGLE version authority for the hedgekit quality
# toolchain (issue #104). Every pip install path applies these pins via
# `-c constraints-quality.txt` (requirements-dev.txt, CI). Pre-commit cannot read a
# constraints file, so `.pre-commit-config.yaml` hook `rev`s and the mypy hook's
# `additional_dependencies` MUST equal these versions — enforced by
# tests/toolchain/test_toolchain_pins.py. When bumping a tool, change it HERE and in
# the matching pre-commit rev in the SAME commit.

# Linters / formatters / type-checkers
ruff==0.15.1
mypy==1.19.1
types-PyYAML==6.0.12.20250915
types-requests==2.33.0.20260518

# Security
bandit==1.9.3
pip-audit==2.10.0

# Complexity
radon==6.0.1
xenon==0.9.3

# Mutation testing
mutmut==2.5.1

# Test runner + coverage
pytest==9.0.2
pytest-cov==7.0.0
pytest-xdist==3.8.0
pytest-asyncio==1.3.0
pytest-mock==3.15.1
pytest-timeout==2.4.0
hypothesis==6.156.1
coverage==7.13.4

# Pre-commit runner
pre-commit==4.5.1
