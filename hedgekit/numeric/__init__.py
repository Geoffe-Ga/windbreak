"""Shared component: numeric primitives for probabilities and prices.

Houses the deterministic numeric helpers (e.g. probability and price
arithmetic) shared across the pipeline and risk components. Concrete
implementations land in a later issue; this package reserves the shared
import path.
"""
